int soma(int a, int b)
{
    return a + b;
}

int main(int argc, int* argv)
{
    int a;
    int b;
    int c[];

    for(i = 0; i < 10; i++)
    {
        c[i] = soma(a, i);
    }

    if(a < 10)
    {
        if(b < 10)
        {
            a = 10;
        }
    }
    else
    {
        b = 1 - ((4*a/4)%5);
    }

    while(i < 10)
    {
        c[3] = *a;
    }

    c = ( a ^ b & c | a ) || asd && qwe;

    return 0;
}